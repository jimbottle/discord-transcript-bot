#Discord Voice
# Pinned to fix/voice-rec-2 branch for DAVE receive support (PR #3159).
# Switch back to a released version once 2.8.0 ships with the receive PR merged.
py-cord[voice] @ git+https://github.com/Pycord-Development/pycord@fix/voice-rec-2

aio-pika
pyyaml

# Envinroment file .env
python-dotenv

# Report Generators
reportlab

# Lets keep our code clean
black
pylint
pytest

#openai and speech recognition
openai
faster_whisper
speechrecognition

# Apple Silicon GPU (Metal) transcription backend (discord-transcript-bot-d8g).
# Selected automatically on Darwin/arm64 (ASR_BACKEND=auto); elsewhere the bot
# falls back to faster-whisper, so these are scoped to Apple Silicon to keep
# Linux/CI installs unaffected. scipy gives an anti-aliased 48k->16k resample
# (protects WER) for the MLX path.
mlx-whisper; sys_platform == 'darwin' and platform_machine == 'arm64'
scipy; sys_platform == 'darwin' and platform_machine == 'arm64'

# LLM for /ask command. >=0.5.1: /ask passes think= to chat(), which
# older clients reject with TypeError (misreported as a daemon failure).
ollama>=0.5.1

# torch support of cuda 12
#--extra-index-url https://download.pytorch.org/whl/cu121
#torch

# torch cpu only support
--extra-index-url https://download.pytorch.org/whl/cpu
torch

# Web UI
flask

