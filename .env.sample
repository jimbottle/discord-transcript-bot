# For Discord
DISCORD_BOT_TOKEN=
DISCORD_CHANNEL_ID=
PLAYER_MAP_FILE_PATH="./player_map.yml"

# Optional: Ollama model used by the /ask command. Defaults to
# ai/mistral:latest if unset (prior behavior). Model selection is
# benchmarked in the sibling local-models repo
# (prompts/discord_ask.json); its current pick is gemma4:26b.
# ASK_OLLAMA_MODEL=ai/mistral:latest